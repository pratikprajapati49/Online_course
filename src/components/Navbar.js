import React from "react";
import './Navbar.css';

const Navbar = () => {
    return (
        <div className="navbar">
            <nav>
                <h1 className="Heading">Top Courses</h1>
            </nav>
        </div>
    );
};

export default Navbar;
